<% @features_with_parents.collect do |a|
     f = a[0]
     depth = a[1]
     views = @view.instance_of?(Array) ? @view : [@view]
     names = @view.collect{ |v| f.prioritized_name(v).name }.uniq %>
<%= depth==0 ? '*' : '  ' * depth %><%=  names.join('; ') %> {<%= f.fid %>}
<% end %>