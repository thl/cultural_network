<% add_breadcrumb_item ts('feature.unused_ids') %>
<section class="panel panel-content">
  <div class="panel-heading">
    <h6><%= ts 'feature.unused_ids' %></h6>
  </div>
  <div class="panel-body">
<p><%=
  # @feature_count
  @features.collect(&:fid).sort.join('<br/>').html_safe
%></p>
<p>&nbsp;<br/>
<%= new_item_link new_admin_feature_pid_path, ts('new.record', :what => t('feature.range')) %> |
<%= link_to ts('back.to', :what => t('feature.id_generator')), admin_feature_pids_path %> |
<%= link_to ts('back.to', :what => t(:admin)), admin_root_path %>
</p>
  </div> <!-- END panel-body -->
</section> <!-- END panel -->
