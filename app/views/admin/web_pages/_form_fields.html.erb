<% if object.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(object.errors.count, "error") %> prohibited this web page from being saved:</h2>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<fieldset>	
  <legend><%= ts(:for, :what => WebPage.model_name.human(:count => :many), :whom => t('citation.of', :what => object.citation.citable.id)) %></legend>
  <div class="row">
<%= f.label(:title).s %>
<%= f.text_field :title, :class => :text %>
  </div>
  <div class="row">
<%= f.label(:path).s %>
<%= object.web_url %><%= f.text_field :path, :class => :text %>
  </div>
</fieldset>